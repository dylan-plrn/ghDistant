Algo HelloWorld { 
	fonction principale {
		// Création de la fenetre
		Créer une fenetre de titre HELLO_WORLD_TITLE 
		Mettre la dimension à 400, 400 
		Configurer la fenetre sur ‘exitOnClose’ 
		// Création du label de texte
		Créer un label avec du texte centré et contenant le texte HELLO_WORLD_TEXT 
		Créer un label contenant la date du jour
		Créer la couleur jaune
		Créer le couleur bleu
		Créer la couleur vert
		Crérr la couleur cyan
		Mettre la couleur jaune sur le label                
		Mettre le fond d'écran en cyan
		Mettre la couleur bleu sur le label
		// Finalisation
		Ajouter le lable sur la fenetre 
		Afficher la fenêtre
		// nouveau commentaire
		// Commentaire en local
	}
}
